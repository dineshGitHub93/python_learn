# Exercise 1 To find Area of Rectangle (A = wl)

length = float(input("Enter the length ? "))
width = float(input("Enter the width ? "))

area = length * width
#Super script 2 -> Num lock + Alt + 0178
print(f"Area of rectangle for {length} & {width} is {area} cm²")
