# To find the circumference of circle using 2*pi*2

import math

radius = float(input("Enter the radius of a circle : "))

circumference = 2 * math.pi * radius

print(f"The Circumference of circle is : {round(circumference, 3)}cm")